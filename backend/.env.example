# 後台管理者帳號密碼，容器啟動時載入為環境變數
# 使用前請複製為 .env（.env 已加入 .gitignore，不會被提交）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# 前台公開頁網址（不含結尾斜線），QR code 會編碼成 {PUBLIC_FRONTEND_URL}/products/{id}
PUBLIC_FRONTEND_URL=http://localhost:5173

# 是否在錯誤回應中附上例外詳細資訊（exception class 名稱），僅開發環境建議開啟
APP_DEBUG=false
