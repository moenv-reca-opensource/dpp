# API 共同前綴。預設 /api 走 vite.config.ts 的 dev proxy（與後端同源，session cookie 才送得出去）；
# 正式部署請把前端 dist 與後端 API 掛在同一個網域下，維持相對路徑即可。
VITE_API_BASE_URL=/api

# 開發時 dev proxy 的後端目標，對應 opensource/docker-compose.yml 的 nginx 對外埠。
VITE_DEV_API_TARGET=http://localhost:8080

# 開發模式：設為 true 可跳過登入，並改用瀏覽器內建的假後端與假資料，
# 完全不需要啟動 opensource/backend。資料存在記憶體，重新整理即回到初始狀態。
# 僅在 npm run dev 生效；npm run build 會把整段繞過邏輯移除，成品不受此值影響。
VITE_DEV_BYPASS_AUTH=false

VITE_APP_BASE_PATH=/
VITE_APP_TITLE=產品數位護照開源後台
